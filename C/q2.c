#include <stdio.h>

int main() {
    int a, b, sum;
    // Input two numbers from the user
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    // Calculate the sum of the two numbers
    sum = a + b;
    // Print the result
    printf("Sum: %d\n", sum);
    return 0;
}
